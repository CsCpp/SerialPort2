<Window x:Class="BatterySerialMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BatterySerialMonitor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!-- Инициализируем ViewModel прямо в XAML -->
    <Window.DataContext>
    <local:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#F0F0F0" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Порт:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding AvailablePorts}" 
                          SelectedItem="{Binding SelectedPort}" Width="120" Margin="0,0,15,0"/>

                <TextBlock Text="Baud:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding AvailableBaudRates}" 
                          SelectedItem="{Binding SelectedBaud}" Width="90" Margin="0,0,15,0"/>

                <!-- Привязка команды вместо Click -->
                <Button Content="Подключить" 
                        Command="{Binding ConnectCommand}" 
                        Width="120" Background="AliceBlue"/>
            </StackPanel>
        </Border>

        <!-- Привязка текста лога -->
        <TextBox Grid.Row="1" Margin="10" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" Background="#1E1E1E" Foreground="LightGreen"
                 Text="{Binding LogText, Mode=OneWay}"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
